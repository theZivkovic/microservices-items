apiVersion: v1
kind: Service
metadata:
  name: {{ include "items-charts.fullname" . }}
  labels:
    {{- include "items-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "items-charts.selectorLabels" . | nindent 4 }}
