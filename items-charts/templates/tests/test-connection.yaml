apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "items-charts.fullname" . }}-test-connection"
  labels:
    {{- include "items-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "items-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
